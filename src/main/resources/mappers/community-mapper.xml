<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="CommunityMapper">
 	<insert id="uploadFile" parameterType="File">
	INSERT INTO COMMUNITY_FILE VALUES(SEQ_FILE.NEXTVAL,'user01',#{fileName},#{fileRename},#{fileSize},DEFAULT,SEQ_POST.CURRVAL)
	</insert>
	<insert id="insertPost" parameterType="Post">
	INSERT INTO COMMUNITY_POST VALUES(SEQ_POST.NEXTVAL,0,'user01','김제탈',#{postTitle},#{postContents},0,DEFAULT,DEFAULT)
	</insert>
 	 </mapper>
  